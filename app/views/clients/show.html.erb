<p style="color: green"><%= notice %></p>
<div id="clients" class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 xl:grid-cols-4 gap-4">
  <div class="flex flex-col items-center justify-center bg-white p-4 shadow rounded-lg">
    <div class="inline-flex shadow-lg border border-gray-200 rounded-full overflow-hidden h-40 w-40">
    <img src="https://images.unsplash.com/photo-1494790108377-be9c29b29330?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=facearea&w=128&h=128&q=60&facepad=2"
        alt=""
        class="h-full w-full">
    </div>
    
    <h2 class="mt-4 font-bold text-xl"><%= @client.name %></h2>
    <h6 class="mt-2 text-sm font-medium"><%= @client.slug %></h6>
    <p class="mt-2 text-sm font-medium"><%= @client.email %></p>
    
    <p class="text-xs text-gray-500 text-center mt-3">
      Lorem ipsum dolor sit amet, consectetur adipisicing elit. Ab enim molestiae nulla.
    </p>
    <% if request.subdomain == 'seller' %>
    <div>
      <%= link_to "Edit this seller", edit_seller_path(@client) %> |
      <%= link_to "Back to sellers", seller_home_path %>
      <%= button_to "Destroy this seller", @client, method: :delete %>
    </div>
    <div>
      <%= link_to "Create product with this seller", new_product_seller_path %>
    </div>
    <% elsif request.subdomain == 'buyer' %>
    <div>
      <%= link_to "Edit this buyer", edit_buyer_path(@client) %> |
      <%= link_to "Back to buyers", buyer_home_path %>
      <%= button_to "Destroy this buyer", @client, method: :delete %>
    </div>
    <div>
      <%= link_to "Buy product with this buyer", new_product_path %>
    </div>
    <% else %>
    <div>
      <%= link_to "Back to clients", clients_path %>
    </div>
    <% end %>
  </div>
</div>